/**
 * Layout del route group `(app)`. Dueño: H5 · packages/ui.
 * El shell del producto: sidebar, header, acento por área.
 *
 * Andamio de H1: un passthrough válido para que el grupo renderice hoy.
 */
export default function GroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
